name: pubspec_ui
description: Manage pubspecs yaml/lock file
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.3.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_lints: ^2.0.0
  path: ^1.8.3
  url_launcher: ^6.1.11
  yaml: ^3.1.2
  http: ^1.0.0
  html: ^0.15.3
  provider: ^6.0.5
  async: ^2.11.0
  golden_toolkit: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
